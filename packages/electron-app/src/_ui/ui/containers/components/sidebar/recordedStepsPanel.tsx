import React from "react";
import { css } from "@emotion/react";

const RecordedStepsPanel = ({ className, ...props }) => {
	return <div className={`${className}`} css={containerStyle}></div>;
};

const containerStyle = css``;

export { RecordedStepsPanel };
